/**
 * Допуски вида провода — ЕДИНЫЙ источник для трёх мест, которые раньше держали свои
 * числа: поле инспектора, санитайзер meta (`LINK_META_FIELDS`) и валидация «липких»
 * настроек нового провода. Границы разъезжались: провод толщиной 30 из чужого архива
 * проходил в модель (дескриптор допускал до 40), показывался в поле, но в липкие
 * настройки не попадал (там потолок был 20).
 */
export const WIRE_STROKE_MIN = 0.5
export const WIRE_STROKE_MAX = 20

/** Виды наконечника: `solid` — треугольник, `open` — две линии под 45°. */
export const ARROW_KINDS = ['solid', 'open']
