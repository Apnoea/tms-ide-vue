// Самодельные иконки инструментов: в PrimeIcons нет ни глифа «текст», ни ломаной, а
// `pi-pencil`/`pi-chart-line` читаются как «редактировать» и «график». Рисуем в их
// манере — сетка 16×16, цвет из `currentColor` (кнопка сама задаёт severity), контур
// как у `pi-stop`/`pi-circle`. Тот же приём, что у иконок выравнивания в
// CanvasInspector.
//
// Иконка — список элементов `{ d, mode }`: `stroke` рисует контур (рамка, ломаная),
// `fill` — сплошную форму (глиф буквы). Рендер — в тулбарах через слот `#icon`.

/**
 * Подпись: буква «Т» в рамке — «текстовый блок», а не «карандаш». Рамка растянута
 * почти на всю сетку, а глиф занимает ~2/3 её высоты: в 14px кнопке мелкая буква
 * сливается в точку.
 */
export const TEXT_ICON = [
  { d: 'M2 2.2h12v11.6H2z', mode: 'stroke' },
  { d: 'M4.4 4.8h7.2v1.7H8.85V11.6h-1.7V6.5H4.4z', mode: 'fill' },
]

/** Ломаная: три звена с изломами — форма читается даже в 14px. */
export const POLYLINE_ICON = [{ d: 'M2 12.2 6 6.2l3.6 3L14 3.8', mode: 'stroke' }]
